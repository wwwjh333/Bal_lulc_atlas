# Assets

Place the following images in this folder before publishing:

- `framework.png` — simplified pipeline diagram (recommended 1600×700 px)
- `data_examples.png` — four-region RGB/GT examples (recommended 1600×850 px)

The page references these filenames in `docs/index.html`.
